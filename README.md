# SIEM con Pandas

Este proyecto es un **SIEM casero** desarrollado en Python que utiliza la librería **Pandas** para analizar un archivo de logs (`logs.csv`) y generar alertas cuando se detectan múltiples intentos fallidos de inicio de sesión.

## 📂 Archivos del proyecto
- `siem_pandas.py`: Script principal que procesa los logs y genera reportes.
- `logs.csv`: Archivo de ejemplo con eventos de prueba (puede ser reemplazado por tus propios logs).

## 🚀 Requisitos
- Python 3.14 (o superior)
- Librería Pandas

## 📦 Instalación de dependencias
```bash
pip install pandas

